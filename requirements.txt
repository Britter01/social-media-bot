# Core
python-dotenv>=1.0.1
Pillow>=10.1.0
numpy>=1.24.0                      # scene cover: screen detection + perspective math

# Content generation — Claude API
anthropic>=0.49.0
pydantic>=2.6.0

# Thumbnails — Google Imagen 4 Fast
google-genai>=1.0.0

# Database — Supabase
# Pinned below 2.16 — storage3>=2.16 pulls in pyiceberg which fails to build
supabase==2.15.0

# Video + HTTP
httpx>=0.27.0

# Publishing
requests>=2.31.0
requests-oauthlib>=2.0.0          # X / Twitter OAuth 1.0a
google-api-python-client>=2.130.0  # YouTube Data API v3
google-auth>=2.30.0                # YouTube OAuth credentials

# Scheduling
APScheduler>=3.10.4
tzdata>=2024.1                     # zoneinfo data (Windows / slim images)

# Reels video generation
# Pinned <2.0 — moviepy 2.x removed the moviepy.editor module and renamed the
# whole clip API (set_duration→with_duration, volumex→with_volume_scaled, …),
# which ReelsAgent relies on. imageio-ffmpeg bundles a static ffmpeg binary so
# rendering works even when no system ffmpeg is on PATH.
moviepy>=1.0.3,<2.0.0
imageio-ffmpeg>=0.4.9
